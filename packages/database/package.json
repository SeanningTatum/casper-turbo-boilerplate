{
  "name": "database",
  "version": "0.0.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "build": "pnpm run generate",
    "generate": "pnpm dlx prisma generate"
  },
  "devDependencies": {
    "@types/node": "^17.0.12",
    "config": "workspace:*",
    "prisma": "^5.8.1"
  },
  "dependencies": {
    "@prisma/client": "^5.8.1"
  }
}
